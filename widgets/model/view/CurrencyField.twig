{% if widget.model[widget.attribute] %}
    <{{ widget.tag }} {{ html.renderTagAttributes(widget.options) }}>{{ widget.currencySymbol }}{{ widget.model[widget.attribute] | number_format(widget.decimalPlaces, widget.decimalSeparator, widget.thousandsSeparator) }}</{{ widget.tag }}>
{% endif %}